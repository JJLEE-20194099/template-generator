import {
    Body,
    Controller,
    Get,
    HttpCode,
    HttpStatus,
    Param,
    Post,
    Query,
    Request,
    UseGuards,
} from '@nestjs/common';
  
import { Types } from 'mongoose';

@Controller('template')
export class TemplateController {
    
}
